CMAKE_MINIMUM_REQUIRED(VERSION 2.4.5)

PROJECT(vtkOFFReader)

FIND_PACKAGE(VTK REQUIRED)
INCLUDE(${VTK_USE_FILE})

ADD_EXECUTABLE(vtkOFFReader test.cpp vtkOFFReader.cxx)
TARGET_LINK_LIBRARIES(vtkOFFReader ${VTK_LIBRARIES})

